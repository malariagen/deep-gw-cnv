# bin — legacy directory

> **This directory has been superseded.**
>
> - Entry-point scripts (train, evaluate, predict, preprocess) now live in **`scripts/`**.
> - Augmentation library code lives in **`src/deep_gw_cnv/augmentation/`**.
> - Validation / evaluation logic lives in **`src/deep_gw_cnv/`** and is invoked via **`scripts/evaluate.py`**.

This directory is kept for historical reference only and may be removed in a future clean-up.