# models — legacy directory

> **This directory has been superseded.**
>
> Model definitions now live in the installable package at
> **`src/deep_gw_cnv/models/`**. See that directory for the current
> architecture definitions and the model registry.

This directory is kept for historical reference only and may be removed.