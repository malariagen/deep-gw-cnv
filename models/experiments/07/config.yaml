# Versioned components
architecture: "01_conv_vae"
hmm:          "02_gaussian_hmm"
cnv:          "03_gene_cnv_caller"   # bin-count-gated CRR fallback
evaluation:   "02_pf9_evaluation"

# Data
store_path: "../../../data/inputs/Pf9-53973-samples-1000bp-core-npy"
out_dir:    "../../../data/results/07_higher_self_transition"
normalise:  true

# Model — reuses experiment 06 checkpoint; no retraining required.
latent_dim:    10
batch_size:    128
epochs:        300
lr:            1.0e-3
weight_decay:  1.0e-5
max_beta:      1.0
warmup_epochs: 50
patience:      10
cnv_downsample_ratio: 0.25  # unchanged from exp 06

# HMM segmentation
# self_transition raised 0.75 → 0.80: exp 06 introduced 142 CRT FPs and 137
# PM2_PM3 FPs with CRR p50 ~1.21 — borderline samples the improved VAE now
# emits a detectable amplification signal for. The HMM self_transition=0.75
# was calibrated for the weak exp 05 VAE; with stronger latent signal we can
# afford a slightly higher self_transition to filter these borderline calls
# while keeping the clear TPs (TP p50 CRR ~1.63–1.88).
hmm_n_states:          6
hmm_self_transition:   0.80   # was 0.75
hmm_low_cov_threshold: 10
hmm_n_jobs:            -1

# CNV calling
cnv_min_cn1_proportion:    0.55
cnv_min_confidence:        0.50
cnv_flank_padding:         100000
cnv_crr_amp_threshold:     1.30   # unchanged — not tuned to labelled data

# CRR fallback only for genes < 3 bins (GCH1 ~2 bins); HMM handles the rest.
cnv_crr_min_bins_fallback: 3

# Evaluation
pf9_gt_path:   "../../../assets/20260313_full_cnv_data_pf9.tsv"
pf9_meta_path: "../../../assets/Pf_9_samples_20260227.txt"
# Minimum evaluable samples for a (Year, Population) group to appear in the
# report. Suppresses tiny groups that produce noisy estimates.
eval_min_group_n: 10
