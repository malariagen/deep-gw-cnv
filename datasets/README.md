# datasets — legacy directory

> **This directory has been superseded.**
>
> Dataset code now lives in the installable package at
> **`src/deep_gw_cnv/datasets/`**. See that directory for Dataset classes,
> the dataset registry, and the `build_loaders()` factory.

This directory is kept for historical reference only and may be removed.
